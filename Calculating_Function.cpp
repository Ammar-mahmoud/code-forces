#include<bits/stdc++.h>

using namespace std;
#define more ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);
int main(){
    more
    long long int num  ;
    cin >> num;
    if (num % 2 == 1)
    {
        cout << -1 *((n +1)/2) ;
    }
    else
    {
        cout << (n/2) ;
    }
}